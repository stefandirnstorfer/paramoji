version: '3'
services:
  backend:
    build: .
    environment:
      MW_SECRET: ${MW_SECRET}
      MONGODB: mongodb://mongodb:27017
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - mongodb
  mongodb:
    image: "mongo:3.6"